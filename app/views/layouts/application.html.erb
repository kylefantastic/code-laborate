<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>&lt;code&gt;laborate</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">

  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js" integrity="sha384-0mSbJDEHialfmuBBQP6A4Qrprq5OVfW37PRR3j5ELqxss1yVqOtnepnHVP9aJ7xS" crossorigin="anonymous"></script>

<meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>
   <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
   <div class="jumbotron">
    <div class="container">
      <h1> &lt;code&gt;laborate </h1>
    </div>
    <!-- logged in/out and link to user -->
  <% if current_user %>
     <% if current_user.org_affiliate %>
      <p class="navbar-text navbar-right">Logged in as <a href= '/organizations/<%= current_user.organization_id %>' class="navbar-link" id="profile-link"><%= current_user.email %></a></p>
      <% else %>
        <p class="navbar-text navbar-right">Logged in as <a href= "/users/<%= current_user.id %>" class="navbar-link"><%= current_user.email %></a></p>
      <% end %>
    <p class="navbar-text navbar-right"><%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %> </p>
  <% end %>
  </div>






  <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger"><%= alert %></p>
  <% end %>

  <div class="container wrapper">

    <nav>
    <ul id='nav-bar' class="nav nav-pills">
     <li role="presentation" class="active"><%= link_to " Home", "/", :class => 'navbar-link' %> </li>
     <li role="presentation" class="active"><%= link_to " About", "/about", :class => 'navbar-link' %> </li>
     <li role="presentation" class="active"><%= link_to " FAQ", "/FAQ", :class => 'navbar-link' %> </li>
     <li role="presentation" class="active"><%= link_to " Past Projects", "/past_projects", :class => 'navbar-link' %> </li>

    <% if current_user %>
      <% if !current_user.org_affiliate %>
        <li role="presentation" class="active"><%= link_to 'Projects', projects_path, :class => 'navbar-link' %> </li>
      <% end %>

    <% else %>
      <li role="presentation" class="active"><%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link nav-bar-right'  %> </li>
      <li role="presentation" class="active"><%= link_to "Login", new_user_session_path, :class => 'navbar-link nav-bar-right'  %> </li>

    <% end %>
    </ul>
  <nav>
    <% if current_user %>
      <% if !current_user.org_affiliate %>
         <div>
            <%= form_tag(projects_path, :method => "get", class: "navbar-form", id: "search-form") do %>
                  <div class="input-append">
                    <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Projects" %>
                    <!-- In order to have the "search" icon int the button, we need to use plain HTML instead
                               of using a Rails form helper -->
                    <button class="btn" type="submit">Search</button>
                  </div>
            <% end %>

         </div>
       <% end %>
     <% end %>



    <%= yield %>
    <div class="push"></div>
  </div>
  <div class="footer">
    <footer><h1>&lt;&#47;code&gt;</h1></footer>
  </div>
</body>
</html>
